export type TUserName = {
    firstName: string;
    lastName: string;
}

export type TShippingAddress = {
    street: string;
    city: string;
    state: string;
    postalCode: string;
    country: string;
};